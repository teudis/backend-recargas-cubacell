@using SmartSolucionesCuba.SAPRESSC.Core.Common.Models.View;
@using SSC.CustomSolution.CubansConexion.TuneUpResell.WebApplication.Models.View;

@model AccountInputViewModel;

@{
    ViewData["Title"] = "Modificar Cuenta";

    ViewData[BreadcrumbViewModel.BREADCRUMBS_MAP_KEY] = new List<BreadcrumbViewModel>
{
new BreadcrumbViewModel
{
Caption = "<span class=\"glyphicon glyphicon-home\"></span> Inicio",
Url = "/"
},
new BreadcrumbViewModel
{
Caption = "Modificar Cuenta",
Url = Url.Action("index", "management", new { Area = string.Empty })
},
new BreadcrumbViewModel
{
Caption = "GruposT",
Url = Url.Action("index")
},
new BreadcrumbViewModel
{
Caption = "Modificar"
}
};

    Layout = "_BackendLayout";
}



@using (Html.BeginForm("edit", "accounts", new { Area = "main", key = string.Empty }, FormMethod.Post, true, new { enctype = "multipart/form-data" }))
{
    @Html.ValidationSummary(true)

    @Html.HiddenFor(model => model.Id)
<section id="mainrender">
    <div class="container">
        <h3>@ViewData["Title"]</h3>
        <div class="row">
            <div class="col-md-8">
                <hr />
                <div class="panel panel-default">
                    <div class="panel-body">

                        <div style="margin-bottom: 20px;">
                            @Html.LabelFor(model => model.Name, new { @class = "control-label" })
                            @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.Name, null, new { @class = "text-danger" })
                        </div>

                        <div class="row">
                            <div class="col-md-6" style="margin-bottom: 20px;">
                                @Html.LabelFor(model => model.Balance, new { @class = "control-label" }) (Opcional)
                                @Html.TextBoxFor(model => model.Balance, new { @class = "form-control" })
                                @Html.ValidationMessageFor(model => model.Balance, null, new { @class = "text-danger" })
                            </div>
                        </div>

                            <div style="margin-bottom: 20px;">
                                <div class="checkbox">
                                    <label>
                                        @Html.EditorFor(model => model.Enabled) Habilitada
                                        @Html.ValidationMessageFor(model => model.Enabled)
                                    </label>
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-md-6" style="margin-bottom: 20px;">
                                    @Html.LabelFor(model => model.RepresentativeId, new { @class = "control-label" })
                                    @Html.DropDownListFor(model => model.RepresentativeId, Model.Users, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(model => model.RepresentativeId, null, new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="pull-left">
                                <a href="@Url.Action("index")"><i class="fa fa-chevron-circle-left"></i> Volver al listado</a>
                            </div>

                            <div class="pull-right">
                                <button type="submit" class="btn btn-success">Guardar</button>
                                <a class="btn btn-secondary" href="@Url.Action("index")">Cancelar</a>
                            </div>
                        </div>
                </div>
            </div>
        </div>
    </div>
</section>
}
